[
    "title": "{{vw.title}}",
    "pages": {
        {% for page in vw.pages.all %}
            { 
                "slug": "{{page.slug}}",
                "title": "{{page.title}}"
            },
        {% endfor %}
    }
]
